{
  "name": "auth-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --host 0.0.0.0 --port 5700",
    "build": "npm run validate:build-env && nuxt build",
    "preview": "nuxt preview --host 0.0.0.0 --port 5700",
    "typecheck": "nuxt typecheck",
    "validate:build-env": "node scripts/validate-build-env.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "nuxt": "^3.16.0",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^5.8.0",
    "vue-tsc": "^3.2.9"
  }
}
